{{ $env := (ds "env") }}
apiVersion: v1
kind: Secret
metadata:
  name: drone-secret
  namespace: {{ $env.namespace }}
type: Opaque
stringData:
{{- $secretsYaml := $env.secrets | toYAML | indent 2 }}
{{ $secretsYaml }}